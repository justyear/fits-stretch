Processed with Stretch, a one-page FITS autostretch that runs in the browser.
Nothing was uploaded. The file never left this machine.

Processing log — fixture-edge.fit
400 × 300 · 32-bit float · 3 channels · 2026-09-05

• Read the FITS: 32-bit IEEE float, 3 image planes, rows stored top-down. Pixel values were already on a 0–1 scale, and the data spans 0.01041 to 1.00000.
• No colour filter array: the file carries 3 separate colour planes (NAXIS3 = 3) and is already demosaiced. No debayer applied.
• Data is linear: median 0.01568, no stretch recorded in the header. Full autostretch applied.
• Background extraction: measured the sky in 108 boxes of 25 pixels on a 12 × 9 grid and used 64 of them (6 brighter than the background, 38 on the frame edge were left out). A thin-plate spline through those points is the model that was removed.
    Each channel got its own model median back as a pedestal (R 0.01696, G 0.01541, B 0.01420), so the background level is preserved and only its variation was removed.
    The ratio between channels is therefore unchanged: no colour grading, no white balance, nothing was decided about the colour of the sky.
• Colour calibration: the sky background was levelled between channels (R 0.001431, G -0.000109, B -0.001323 subtracted, additively, so only the floor moved).
    Stellar gains: the faintest star median is 0.040499, less than 3x the sky pedestal (0.015563); a ratio taken above the sky is unstable when the star is barely above it.
    Star selection is a brightness cut, not star detection: bright unsaturated pixels are predominantly stars in a star field. Pixels whose neighbourhood is also mostly selected are rejected as extended, which removes a galaxy core but would also remove a star field dense enough to look like one.
• Autostretch — midtones transfer function (PixInsight STF / Siril autostretch), shadow clip -2.80σ, target background 0.085 — that is 22 of 255. Applied LINKED: one curve, derived from the luminance, and every channel multiplied by the same number. Highlights untouched at 1.000.
    Luminance  median 0.01552  MADN 0.00050  →  shadows 0.01412  midtones 0.01503
    Because one curve governs all three channels, the ratio between them is unchanged by construction. Measured on this frame, the largest change to any channel ratio was 4.4e-16 across 120,000 pixels — float rounding, nothing else. In the highlights, R/G 1.0241 → 1.0215 and B/G 0.9962 → 0.9970.
    1,769 pixels came out above 1.0 in one channel. All three were divided by their own maximum rather than the bright channel being clipped on its own: clipping one channel changes the colour of the pixel, dividing takes it to white and keeps it.
    0.000% of pixels were clipped by the transfer itself — luminance at or below the black point, or at or above 1.0. That is a different count from the line above: this one is the curve, that one is the overflow.
• Output: 8-bit sRGB, full resolution, no resampling. Rounded with ±0.5 of a level of dither, from the fixed seed 20260906, which breaks the banding a subtracted surface would otherwise leave. The seed is fixed, so the same file always produces the same image.
• Not applied: noise reduction, sharpening, saturation, deconvolution, star removal, colour grading, or any AI or generative step.

Every number above was measured from the file itself.